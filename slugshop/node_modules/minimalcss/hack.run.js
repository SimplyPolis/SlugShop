const minimalcss = require('./index');
console.log(minimalcss.version);
minimalcss
  .minimize({
    urls: ['http://peterbecom.local/search?q=searchingness'],
    skippable: request => {
      return !!request.url().match('fonts.googleapis.com');
    }
  })
  .then(output => {
    console.log(output.finalCss);
  });
