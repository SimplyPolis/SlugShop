const removeSequentialSemis = css => {
  while (/;\s*;/.test(css)) {
    css = css.replace(/;\s*;/g, ';');
  }
  return css;
};

CSS = `
  Heres; ; the first one.
  Second ; ; one
  Third ; ; ; one
  FOURTH ;;;;;;
`;
const n = removeSequentialSemis(CSS);
console.log({ n });
