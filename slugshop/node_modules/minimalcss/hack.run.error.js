const minimalcss = require('./index')
console.log(minimalcss.version)
minimalcss
  .minimize({ urls: ['http://peterbecom.dev/xxxcss-blocking/ultra-basic.html'] })
  .then(output => {
    console.log('OUTPUT', output.length, output)
  })
  .catch(errr => {
    console.log('ERR!');
  })
